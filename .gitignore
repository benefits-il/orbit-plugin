# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Node (if adding build tools later)
node_modules/
dist/
.env
.env.local

# Claude
.claude/
!.claude-plugin/

# Misc
*.log
*.tmp

# Never commit a zip into the plugin source — the plugin source is "./" (whole repo),
# so a committed *.zip becomes a nested zip and breaks `marketplace add` with
# "nested zip file are not allowed". Distribute zips as GitHub release assets instead.
*.zip
